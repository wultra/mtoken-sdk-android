name: build

on:
  pull_request:
    branches:
      - develop
      - master

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - run: ./gradlew clean build
